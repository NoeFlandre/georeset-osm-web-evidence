[project]
name = "georeset-osm-web-evidence"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "folium>=0.20.0",
    "geopandas>=1.1.3",
    "openpyxl>=3.1.5",
    "pyarrow>=24.0.0",
    "requests>=2.34.2",
    "shapely>=2.1.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/georeset_osm_web_evidence"]
