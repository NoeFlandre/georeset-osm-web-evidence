# Storage

Local artifact read/write helpers.

- `local.py` saves and loads GeoDataFrames as Parquet files.

The project keeps code in Git and data artifacts outside Git, with local data synchronized to the Hugging Face bucket.
