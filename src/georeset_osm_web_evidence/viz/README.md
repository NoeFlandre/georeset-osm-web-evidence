# Visualization

Lightweight visual quality-assurance utilities.

- `map.py` creates Folium HTML maps from polygon GeoDataFrames.

The current map colors polygons by whether a geolocated Wikipedia article was found inside the polygon.
