# Review

This package prepares artifacts for human inspection and annotation.

It should make review easy without changing the underlying evidence data:

- Build compact CSV files for manual labeling.
- Keep reviewer-facing labels and notes separate from raw fetched text.
- Preserve stable identifiers so labels can later be merged back into pipeline
  outputs.
